{
    "type": "object",
    "description": "A folder in a user's playlist organization.",
    "properties": {
        "id": {
            "type": "string",
            "description": "ID of the playlist folder."
        },
        "name": {
            "type": "string",
            "description": "Name of the playlist folder."
        },
        "parentId": {
            "type": "string",
            "description": "ID of the parent playlist folder. Omitted for a root-level folder."
        },
        "sortOrder": {
            "type": "integer",
            "description": "Position among siblings, as a client-assigned integer."
        }
    },
    "required": [
        "id",
        "name"
    ],
    "externalDocs": {
        "description": "PlaylistFolder",
        "url": "https://opensubsonic.netlify.app/docs/responses/playlistfolder/"
    }
}
