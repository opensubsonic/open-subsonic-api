{
    "description": "A Child element with an additional normalized distance field.",
    "allOf": [
        {
            "$ref": "./Child.json"
        },
        {
            "type": "object",
            "properties": {
                "distance": {
                    "type": "number",
                    "description": "The normalized cosine similarity distance (0.0 = identical, 1.0 = most dissimilar). For getSonicSimilarTracks, relative to the query song. For findSonicPath, relative to the starting song.",
                    "minimum": 0,
                    "maximum": 1
                }
            },
            "required": [
                "distance"
            ]
        }
    ]
}
