{
    "description": "A Child element with an additional normalized distance field.",
    "allOf": [
        {
            "$ref": "./Child.json"
        },
        {
            "type": "object",
            "properties": {
                "distance": {
                    "type": "number",
                    "description": "The normalized distance from the reference song (0.0 = identical, 1.0 = most dissimilar).",
                    "minimum": 0,
                    "maximum": 1
                }
            },
            "required": [
                "distance"
            ]
        }
    ]
}
