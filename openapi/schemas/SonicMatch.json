{
    "type": "object",
    "description": "A matched song with its normalized distance.",
    "properties": {
        "id": {
            "type": "string",
            "description": "The ID of the matched song."
        },
        "distance": {
            "type": "number",
            "description": "The normalized distance from the reference song (0.0 = identical, 1.0 = most dissimilar).",
            "minimum": 0,
            "maximum": 1
        }
    },
    "required": [
        "id",
        "distance"
    ]
}
