{
    "type": "object",
    "description": "The result of a sonic similar tracks search.",
    "properties": {
        "sonicMatch": {
            "type": "array",
            "description": "A list of similar songs ordered by similarity, each with its normalized similarity score from the query song.",
            "items": {
                "$ref": "./SonicMatch.json"
            }
        }
    }
}
