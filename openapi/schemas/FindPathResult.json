{
    "type": "object",
    "description": "The result of finding a path between two songs based on audio similarity.",
    "externalDocs": {
        "description": "FindPathResult",
        "url": "https://opensubsonic.netlify.app/docs/responses/findpathresult/"
    },
    "properties": {
        "path": {
            "type": "array",
            "items": {
                "$ref": "./AudioFeatureTrack.json"
            },
            "description": "The ordered list of tracks forming the path from start to end song."
        },
        "totalDistance": {
            "type": "number",
            "description": "The total distance (sum of pairwise distances) of the computed path."
        }
    },
    "required": [
        "path",
        "totalDistance"
    ]
}
