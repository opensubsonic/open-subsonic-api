{
    "type": "object",
    "description": "An album sort key. Sort keys are evaluated in array order.",
    "additionalProperties": false,
    "properties": {
        "field": {
            "type": "string",
            "enum": [
                "id", "name", "version", "artist", "year", "songCount",
                "duration", "playCount", "userRating", "created", "starred",
                "played", "sortName"
            ]
        },
        "direction": {
            "$ref": "./ExtendedLibrarySortDirection.json"
        }
    },
    "required": ["field", "direction"]
}
