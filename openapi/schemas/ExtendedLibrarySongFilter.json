{
    "description": "A recursive song filter node. A node is exactly one leaf rule, non-empty AND group, or non-empty OR group.",
    "oneOf": [
        {
            "$ref": "./ExtendedLibrarySongFilterRule.json"
        },
        {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "and": {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                        "$ref": "./ExtendedLibrarySongFilter.json"
                    }
                }
            },
            "required": ["and"]
        },
        {
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "or": {
                    "type": "array",
                    "minItems": 1,
                    "items": {
                        "$ref": "./ExtendedLibrarySongFilter.json"
                    }
                }
            },
            "required": ["or"]
        }
    ]
}
