{
    "type": "object",
    "description": "A song sort key. Sort keys are evaluated in array order.",
    "additionalProperties": false,
    "properties": {
        "field": {
            "type": "string",
            "enum": [
                "id", "title", "album", "artist", "year", "track",
                "discNumber", "duration", "size", "bitRate", "bitDepth",
                "samplingRate", "channelCount", "playCount", "userRating",
                "averageRating", "bpm", "created", "starred", "played",
                "sortName"
            ]
        },
        "direction": {
            "$ref": "./ExtendedLibrarySortDirection.json"
        }
    },
    "required": ["field", "direction"]
}
