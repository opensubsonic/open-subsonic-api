{
    "type": "object",
    "description": "The result of a sonic path search between two songs.",
    "properties": {
        "song": {
            "type": "array",
            "description": "An ordered list of songs forming the path from the start song to the end song.",
            "items": {
                "$ref": "./Child.json"
            }
        }
    },
    "required": [
        "song"
    ]
}
