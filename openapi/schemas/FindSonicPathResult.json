{
    "type": "object",
    "description": "The result of a sonic path search between two songs.",
    "properties": {
        "id": {
            "type": "array",
            "description": "An ordered list of song IDs forming the path from the start song to the end song.",
            "items": {
                "type": "string"
            }
        }
    },
    "required": [
        "id"
    ]
}
