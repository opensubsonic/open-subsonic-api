{
    "type": "object",
    "description": "The result of a sonic path search between two songs.",
    "properties": {
        "sonicMatch": {
            "type": "array",
            "description": "An ordered list of songs forming the path from the start song to the end song, each with its distance from the start song.",
            "items": {
                "$ref": "./SonicMatch.json"
            }
        }
    },
    "required": [
        "sonicMatch"
    ]
}
